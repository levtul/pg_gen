CREATE TABLE a
(
    x INT PRIMARY KEY
);

CREATE TABLE b
(
    x INT REFERENCES a PRIMARY KEY
);

CREATE TABLE c
(
    x INT REFERENCES b PRIMARY KEY
);

ALTER TABLE a
    ADD CONSTRAINT fkk FOREIGN KEY (x) REFERENCES c (x);